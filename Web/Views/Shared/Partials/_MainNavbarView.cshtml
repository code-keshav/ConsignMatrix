@using Base.Providers.Interfaces

@inject ICurrentUserProvider UserProvider;

@{
    var user = await UserProvider.GetCurrentUser();
    var branch = await UserProvider.GetUserBranch();
    var userName = user.UserName ?? "";
    var name = user.Name;
    var branchName = branch.Name;
}

<nav class="app-header navbar navbar-expand-lg bg-body">
    <div class="container-fluid">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" data-lte-toggle="sidebar" href="#" role="button">
                    <i class="fa fa-bars"></i>
                </a>
            </li>
        </ul>

        <!-- Right navbar links -->
        <ul class="navbar-nav ms-auto">
            @* <li class="nav-item dropdown"> *@
            @*     <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" data-bs-auto-close="outside" role="button" aria-expanded="false"> *@
            @*         <i class="bi bi-translate me-1"></i> *@
            @*         @(System.Globalization.CultureInfo.CurrentUICulture.Name == "ne-NP" ? "नेपाली" : "English") *@
            @*     </a> *@
            @*     <div class="dropdown-menu"> *@
            @*         <a class="dropdown-item" href="@Url.Action("ChangeLanguage", "Home", new { culture = "en", returnUrl = Context.Request.GetDisplayUrl() })"> *@
            @*             English *@
            @*         </a> *@
            @*         <a class="dropdown-item" href="@Url.Action("ChangeLanguage", "Home", new { culture = "ne-NP", returnUrl = Context.Request.GetDisplayUrl() })"> *@
            @*             नेपाली *@
            @*         </a> *@
            @*     </div> *@
            @* </li> *@

            <li class="nav-item">
                <a class="nav-link" href="#" data-lte-toggle="fullscreen">
                    <i data-lte-icon="maximize" class="fa fa-expand"></i>
                    <i data-lte-icon="minimize" class="fa fa-compress" style="display: none"></i>
                </a>
            </li>
            <li class="nav-item dropdown user-menu text-white">
                <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
                    <i class=" fa fa-circle-user me-2"></i>
                    <span class="d-none d-md-inline">@name</span>
                </a>
                <ul class="dropdown-menu dropdown-menu-lg dropdown-menu-end">
                    <li class="user-header text-bg-primary">
                        <i class="fa fa-circle-user" style="font-size: 4rem;"></i>
                        <p>
                            @userName
                            <small>@branchName</small>
                        </p>
                    </li>
                    <li class="user-footer">
                        <a href="/admin/login/logout" class="btn btn-default btn-flat float-end">
                            <i class="fa fa-arrow-right-from-bracket me-1"></i>Sign out
                        </a>
                    </li>
                </ul>
            </li>
        </ul>
    </div>
</nav>
